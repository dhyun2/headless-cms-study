  </svg>
import React from 'react';

type {{name}}IconProps = {
  size?: string;
  color?: string;
};

const {{name}}Icon: React.FC<{{name}}IconProps> = ({ size = '24', color = 'currentColor' }) => (
  {{{svgContent}}}
);

export default {{name}}Icon;
