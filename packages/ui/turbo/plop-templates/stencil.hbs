import { Component, Prop, h } from '@stencil/core';

@Component({
  tag: '{{name}}',
  styleUrl: '{{name}}.css',
  shadow: true,
})
export class {{name}} {
  @Prop() text: string;

  render() {
    return <div>{this.text}</div>;
  }
}
